PROMPT PADRÃO DE EXECUÇÃO — FINANCE APP (ANTI-DECAIMENTO)
CONTEXTO (IMUTÁVEL)

Você recebeu anteriormente um PRD extenso, fechado e imutável para um aplicativo financeiro offline-first em HTML/CSS/JS.

Esse PRD é o único contrato válido para este projeto.

❗ Você NÃO pode:

Resumir o PRD

Reinterpretar regras

Simplificar lógica

Antecipar funcionalidades de fases futuras

“Improvisar” soluções não descritas

Se algo não puder ser implementado exatamente como o PRD define, você deve PARAR e explicar o problema, em vez de inventar.

TAREFA ATUAL

Execute APENAS a FASE {NÚMERO_DA_FASE} do plano de execução aprovado.

Não escreva código de fases futuras.
Não omita requisitos da fase atual.

REGRAS DE EXECUÇÃO OBRIGATÓRIAS
1) ESCOPO

Implemente somente os componentes listados na FASE {NÚMERO_DA_FASE}.

Qualquer referência a outra fase deve ser apenas em comentário // TODO (fase futura).

2) QUALIDADE TÉCNICA

Código completo, funcional e testável isoladamente.

Nenhum pseudocódigo.

Sem dependências além das explicitadas no PRD.

Valores monetários sempre em centavos (inteiros).

Datas exibidas sempre em dd/mm/aaaa.

3) ORGANIZAÇÃO DO CÓDIGO

Código em um único arquivo (quando aplicável).

Separar logicamente com comentários claros:

// CONFIG / CONSTANTS

// DATA MODELS

// STORAGE (IndexedDB)

// BUSINESS RULES

// UI

// PHASE {NÚMERO_DA_FASE}

Tudo implementado nesta fase deve estar marcado com // PHASE {NÚMERO_DA_FASE}.

4) REGRAS FINANCEIRAS (INVIOLÁVEIS)

Lançamentos pendentes contam como 0 nos totais.

Estornos/reembolsos usam valores negativos.

Receita não tem categoria.

Despesa exige categoria.

Crédito sempre exige cartão específico (quando aplicável).

5) NÃO-ALUCINAÇÃO

Se você:

Não souber implementar algo

Identificar conflito técnico

Perceber ambiguidade

➡️ Pare imediatamente e explique o problema.
➡️ Não continue tentando “resolver do seu jeito”.

FORMATO DE ENTREGA (OBRIGATÓRIO)
A) ENTREGA PRINCIPAL

Código completo referente exclusivamente à FASE {NÚMERO_DA_FASE}

Sem placeholders de funcionalidades futuras (apenas // TODO comentado)

B) CHECKPOINT FINAL (OBRIGATÓRIO)

Ao final da resposta, inclua exatamente este checklist preenchido:

CHECKPOINT — FASE {NÚMERO_DA_FASE}

1) Regras do PRD atendidas nesta fase:
- [ ] Regra 1
- [ ] Regra 2
- [ ] Regra 3

2) Regras do PRD NÃO implementadas nesta fase:
- [ ] Regra A (fase futura)
- [ ] Regra B (fase futura)

3) Riscos técnicos ou simplificações:
- Nenhum / Descrever

4) Confirmação:
Aguardando autorização para executar a próxima fase.

REGRA FINAL (CRÍTICA)

Você NÃO deve:

Concluir a fase “em partes”

Dizer que continuará depois

Pedir desculpas por limitações

Você executa integralmente a fase atual
ou para e explica por que não é possível.
